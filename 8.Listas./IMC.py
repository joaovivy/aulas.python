import os
os.system("cls || clear")

# Função para somar
numeros = []

numeros.append(10)
numeros.append(5)
numeros.append(2)

soma = sum(numeros)

print(f"Soma: {soma}")

# Função para potência.
altura = 1.50

calculo = altura * altura
calculo = altura ** 2
calculo = pow(altura, 2)