"""
Escreva um programa que use um laço `for` para imprimir as letras de 'A' a 'E'
"""
import os
os.system("cls || clear")

palavra = "ABCDE"

# ForEach - Para cada
for letra in palavra:
    print(letra)